A small library implementing a directed graph structure as an Immutable.js `Record`. Its primary purpose is the `topoSort()` method, which returns a lazy `Seq` of the graphs nodes, sorted topologically.
